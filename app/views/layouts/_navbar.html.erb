<nav class="shadow navbar navbar-position navbar-light blue-gradient rounded-bottom mb-5">
  <span class="navbar-text text-white font-weight-bold text-dark-shadow">Simple TODO LISTS for Rubygarage by Valter0ff</span>
  <% if user_signed_in? %>
		<span class="navbar-text text-white font-weight-bold text-dark-shadow">Account: <%= current_user.email %></span>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="fa fa-user-circle-o text-white font-weight-bold text-dark-shadow"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
    <div class="navbar-nav-row justify-content-end">
			<% if user_signed_in? %>
				<%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link text-white font-weight-bold text-dark-shadow" %>
			<% else %>
				<%= link_to "Login", new_user_session_path, class: "nav-link text-white font-weight-bold text-dark-shadow" %>
				<%= link_to "Create account", new_user_registration_path, class: "nav-link text-white font-weight-bold text-dark-shadow" %>
			<% end %>
    </div>
  </div>
</nav>

