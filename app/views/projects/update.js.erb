$("#modal-window").modal('hide');
elem = $('ul#<%=dom_id(@project)%>');
elem.find('span.project-title').text("<%= @project.title %>");
elem.find("div.flatpickr a.input-button").first().attr("title", "<%= project_deadline(@project) %>");
