<div class="modal-header blue-gradient">
  <h5 class="modal-title text-white font-weight-bold text-dark-shadow" id="exampleModalLabel">Enter here project title</h5>
  <button type="button" class="close text-dark-shadow fa-heading-style" data-dismiss="modal" aria-label="Close" aria-hidden="true">×</button>
 </div>
<div class="modal-body light-grey">
  <%= form_with(model: project) do |form| %>
    <% if project.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>
  
        <ul>
        <% project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="field d-flex ">
       <%= form.text_field :title, class:"form-control border-left-0 border-bottom-0 border-secondary" %>
    </div>
    <div class="d-flex actions justify-content-center">
      <%= form.submit class:"btn mt-3 px-3 text-white font-weight-bold border-top  border-secondary green-gradient text-dark-shadow" %>
    </div>
   <% end %> 
</div>




